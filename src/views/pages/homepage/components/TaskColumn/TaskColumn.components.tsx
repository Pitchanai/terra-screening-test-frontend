import { Box, styled } from '@mui/material'

export const Root = styled(Box)(() => ({
  background: 'pink',
  height: '100%',
  maxHeight: '100%',
  padding: 24,
  overflowY: 'auto',
  width: 200,
  minWidth: 200,
}))
